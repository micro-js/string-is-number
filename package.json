{
  "name": "@f/string-is-number",
  "description": "Check is a string looks like a number.",
  "repository": "git://github.com/micro-js/string-is-number.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-number": "^1.1.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
